# @sms-localblast/validation

Zod schemas and parsers for API inputs, config, and integration payloads.

UI and controllers should call parsers from here (or thin wrappers) rather than inlining validation logic.
