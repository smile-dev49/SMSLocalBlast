{
  "name": "sms-localblast",
  "version": "1.0.0",
  "private": true,
  "description": "White-label SMS gateway: Excel → API → mobile",
  "type": "module",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "package:codecanyon": "node scripts/package-codecanyon.js"
  },
  "devDependencies": {
    "archiver": "^7.0.0"
  }
}
