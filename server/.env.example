NODE_ENV=development
PORT=3000

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/sms_localblast

# API base URL (for CORS / add-in — your HTTPS URL in production)
PUBLIC_ORIGIN=http://localhost:3000
