# @sms-localblast/utils

Pure, framework-agnostic utilities (formatting, tenancy helpers, idempotency keys, etc.).

Keep side effects out of this package; IO belongs in app `data` / `features` layers.
