# @sms-localblast/types

Shared TypeScript types and branded identifiers for multi-tenant boundaries.

## Rules

- All cross-app domain shapes should live here (or split into focused subpaths later).
- Apps and UI must not define duplicate domain types; import from this package.
